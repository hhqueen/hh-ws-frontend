import React from 'react'
import {Table} from "flowbite-react"

export default function InputTableComp({headerArr, data, setData, handleButtonClick}) {
  
  
  return (
    <div>TableComp</div>
  )
}
